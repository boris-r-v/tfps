/** \file main.cpp
    @brief Стартер, в лучших традициях в одну(две) строчки
*/
#include <main.h>
int main(int argc, char** argv )
{

    TFPS::Main main;
    main.run( argc, argv );

    return 0;
}
